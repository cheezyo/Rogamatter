<p id="notice"><%= notice %></p>
<!-- Page Heading -->
<div class="d-sm-flex align-items-center justify-content-between mb-4">
    <h1 class="h3 mb-0 text-gray-800"> <%= @company.name %> </h1>

    <div class="row">
	    <div class="col">
	    	<i class="far fa-edit"></i>
	           <%= link_to 'Rediger', edit_company_path(@company) %>     

	     	 <i class="far fa-plus-square"></i>
			   <%= link_to "Legg til ny avtale", new_company_mat_path %>
		</div> 
   	</div>
</div>
<hr/>



<!-- Content Row -->
<% @company.company_mats.each do |m| %>
          <div class="row">

            <!-- Area Chart -->
            <div class="col-xl-8 col-lg-7">
              <div class="card shadow mb-4">
                <div class="card-header py-3">
                  
                  <h6 class="m-0 font-weight-bold text-primary"><%= m.amount %>  matter type  <%= m.mat.name %> </h6>

              </div>
              <div class="card-body">
                  		<strong>Byttes hver </strong>
        						  <%= m.change_day %>, <%= m.change %> ganger i måneden
        						  
        						</p>

        						<p>
        						  <strong>Kontrak starter</strong>
        						  <%= m.start_date %>
        						</p>

        						<p>
        						  <strong>Kontrakt slutter</strong>
        						  <%= m.end_date %>
        						</p>
                    
            	</div>
              <div class="card-footer">
                <i class="far fa-edit"></i>
                <%= link_to "Edit contract", edit_company_mat_path(m) %> 
                <i class="far fa-minus-square"></i>
                <%= link_to 'Delete', m, method: :delete, data: { confirm: 'Are you sure?' } %>
              </div>
                    
                  
          </div>
      </div>
  </div>
 <% end -%>

 